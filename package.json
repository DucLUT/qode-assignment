{
  "name": "qode-assignment-heroku",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "heroku-postbuild": "npm --prefix server install --include=dev && npm --prefix server run build",
    "start": "npm --prefix server start"
  }
}